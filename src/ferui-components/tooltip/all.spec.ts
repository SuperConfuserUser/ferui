import TooltipComponentSpecs from './tooltip.component.spec';
import TooltipDirectiveSpecs from './tooltip.directive.spec';

describe('Tooltip', function () {
  TooltipDirectiveSpecs();
  TooltipComponentSpecs();
});
